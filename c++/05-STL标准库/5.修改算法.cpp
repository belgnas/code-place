//  STL 算法

// 修改算法

// reverse: 反转区间内的元素顺序。

// reverse(vec.begin(), vec.end());
// fill: 将指定区间内的所有元素赋值为某个值。

// fill(vec.begin(), vec.end(), 0);  // 所有元素设为 0
// replace: 将区间内的某个值替换为另一个值。

// replace(vec.begin(), vec.end(), 1, 99);  // 将所有 1 替换为 99
// copy: 将区间内的元素复制到另一个区间。

// vector<int> vec2(6);
// copy(vec.begin(), vec.end(), vec2.begin());