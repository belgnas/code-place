// 队列 （先进先出）

#include<iostream>
using namespace std;

const int N = 100010;

// ************************** 队列

// 在队尾插入元素，在队头弹出元素
int q[N], hh, tt = -1;

// 插入
q[++tt] = x;

// 弹出
hh ++;

// 判断是否为空
if(hh <= tt) not empty;
else empty;

// 取出队头元素 (拿数组模拟的话，不仅可以取出队头元素，还可以取出队尾元素)
q[hh];
// 取出队尾元素
q[tt];

// **************************