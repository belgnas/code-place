// 深度优先搜索(DFS) && 广度优先搜索(BFS)

// 深度优先搜索

// 题目：八皇后问题

// n皇后问题是指将 n 个皇后放在 n?n 的国际象棋棋盘上，使得皇后不能相互攻击到，即任意两个皇后都不能处于同一行、同一列或同一斜线上。
// 现在给定整数n，请你输出所有的满足条件的棋子摆法。

// 输入格式
// 共一行，包含整数n。

// 输出格式
// 每个解决方案占n行，每行输出一个长度为n的字符串，用来表示完整的棋盘状态。
// 其中”.”表示某一个位置的方格状态为空，”Q”表示某一个位置的方格上摆着皇后。
// 每个方案输出完成后，输出一个空行。

// 数据范围
// 1 ≤ n ≤ 9

// 输入样例：
// 4

// 输出样例：
// .Q..
// ...Q
// Q...
// ..Q.

// ..Q.
// Q...
// ...Q
// .Q..


// 第一种搜索方法（按行搜索）

#include<iostream>
using namespace std;

const int N = 20;

int n;
char g[N][N];;
bool col[N], dg[N], udg[N];

void dfs(int u)
{
    if(u == n)
    {
        for (int i = 0; i < n; i++) puts(g[i]);
        puts("");
        return;
    }
    for(int i = 0; i < n; i++)
    {
        if(!col[i] && !dg[u + i] && !udg[n - u + i])
        {
            g[u][i] = 'Q';
            col[i] = dg[u + i] = udg[n - u + i] = true;
            dfs(u + 1);
            g[u][i] = '.';
            col[i] = dg[u + i] = udg[n - u + i] = false;
            
        }
    }
}

int main()
{
    cin >> n;
    for(int i = 0; i < n; i++)
    {
        for(int j =0; j < n; j++)
        {
            g[i][j] = '.';
        }
    }

    dfs(0);

    return 0;
}


// 第二种搜索方法（按格搜索）

// #include<iostream>

// using namespace std;

// const int N = 20;

// int n;
// char g[N][N];
// bool row[N], col[N], dg[N], udg[N];

// void dfs(int x, int y, int s) // x, y分别表示当前处理的格子的行和列坐标。s 表示已经放置的皇后数量
// {
//     if(y == n) y = 0, x++;

//     if(x == n)
//     {
//         if(s == n)
//         {
//             for(int i = 0; i < n; i++) puts(g[i]);
//             puts("");
//         }
//         return;
//     }

//     // 不放皇后
//     dfs(x, y + 1, s);

//     // 放皇后
//     if(!row[x] && !col[y] && !dg[x + y] && !udg[x - y + n])
//     {
//         g[x][y] = 'Q';
//         row[x] = col[y] = dg[x + y] = udg[x - y + n] = true;
//         dfs(x, y + 1, s + 1);
//         g[x][y] = '.';
//         row[x] = col[y] = dg[x + y] = udg[x - y + n] = false;
//     }
// }

// int main()
// {
//     cin >> n;
//     for(int i = 0; i < n; i++)
//     {
//         for(int j = 0; j < n; j++)
//         {
//             g[i][j] = '.';
//         }
//     }

//     dfs(0, 0, 0);

//     return 0;
// }








// ai注释版

//      第一种

// #include<iostream>
// using namespace std;

// // 定义常量 N，用于表示棋盘的最大规模，这里假设棋盘最大为 20x20
// const int N = 20;

// // n 表示棋盘的大小，即 n x n 的棋盘，同时也是皇后的数量
// int n;
// // g 是一个二维字符数组，用于存储棋盘的状态，'.' 表示空位，'Q' 表示皇后
// char g[N][N];
// // col 数组用于标记每一列是否已经放置了皇后，true 表示该列已有皇后
// bool col[N];
// // dg 数组用于标记正对角线（从左上到右下）是否已经放置了皇后
// bool dg[N];
// // udg 数组用于标记反对角线（从右上到左下）是否已经放置了皇后
// bool udg[N];

// // 深度优先搜索函数，u 表示当前正在处理的行号
// void dfs(int u)
// {
//     // 当 u 等于 n 时，说明已经成功处理完了所有的行，即找到了一种合法的皇后放置方案
//     if(u == n)
//     {
//         // 遍历棋盘的每一行
//         for (int i = 0; i < n; i++)
//             // 输出当前行的棋盘状态
//             puts(g[i]);
//         // 输出一个空行，用于分隔不同的方案
//         puts("");
//         // 回溯，继续寻找其他可能的方案
//         return;
//     }

//     // 遍历当前行的每一列
//     for(int i = 0; i < n; i++)
//     {
//         // 检查当前列、当前正对角线和当前反对角线是否都没有皇后
//         if(!col[i] && !dg[u + i] && !udg[n - u + i])
//         {
//             // 如果都没有皇后，则在当前位置放置一个皇后
//             g[u][i] = 'Q';
//             // 标记当前列已经有皇后
//             col[i] = true;
//             // 标记当前正对角线已经有皇后
//             dg[u + i] = true;
//             // 标记当前反对角线已经有皇后
//             udg[n - u + i] = true;
//             // 递归调用 dfs 函数，处理下一行
//             dfs(u + 1);
//             // 回溯操作，撤销当前位置的皇后放置
//             g[u][i] = '.';
//             // 撤销当前列的标记
//             col[i] = false;
//             // 撤销当前正对角线的标记
//             dg[u + i] = false;
//             // 撤销当前反对角线的标记
//             udg[n - u + i] = false;
//         }
//     }
// }

// int main()
// {
//     // 从标准输入读取棋盘的大小 n
//     cin >> n;
//     // 初始化棋盘，将所有位置都置为 '.'，表示空位
//     for(int i = 0; i < n; i++)
//     {
//         for(int j = 0; j < n; j++)
//         {
//             g[i][j] = '.';
//         }
//     }
//     // 从第 0 行开始进行深度优先搜索
//     dfs(0);
//     // 程序正常结束
//     return 0;
// }


//      第二种

// #include<iostream>

// using namespace std;

// // 定义常量 N，用于表示棋盘的最大规模，这里假设棋盘最大为 20x20
// const int N = 20;

// // n 表示棋盘的大小，即 n x n 的棋盘，同时也是皇后的数量
// int n;
// // g 是一个二维字符数组，用于存储棋盘的状态，'.' 表示空位，'Q' 表示皇后
// char g[N][N];
// // row 数组用于标记每一行是否已经放置了皇后，true 表示该行已有皇后
// bool row[N];
// // col 数组用于标记每一列是否已经放置了皇后，true 表示该列已有皇后
// bool col[N];
// // dg 数组用于标记正对角线（从左上到右下）是否已经放置了皇后
// bool dg[N];
// // udg 数组用于标记反对角线（从右上到左下）是否已经放置了皇后
// bool udg[N];

// // 深度优先搜索函数，x 和 y 表示当前正在处理的格子的坐标，s 表示已经放置的皇后数量
// void dfs(int x, int y, int s)
// {
//     // 如果当前列 y 等于 n，说明已经处理完了当前行，需要转到下一行的第 0 列
//     if(y == n)
//     {
//         y = 0;
//         x++;
//     }

//     // 如果当前行 x 等于 n，说明已经处理完了所有的格子
//     if(x == n)
//     {
//         // 检查已经放置的皇后数量是否等于 n，如果等于 n，说明找到了一种合法的方案
//         if(s == n)
//         {
//             // 遍历棋盘的每一行
//             for(int i = 0; i < n; i++)
//                 // 输出当前行的棋盘状态
//                 puts(g[i]);
//             // 输出一个空行，用于分隔不同的方案
//             puts("");
//         }
//         // 回溯，继续寻找其他可能的方案
//         return;
//     }

//     // 情况一：当前格子不放置皇后，直接处理下一个格子
//     dfs(x, y + 1, s);

//     // 情况二：尝试在当前格子放置皇后
//     // 检查当前行、当前列、当前正对角线和当前反对角线是否都没有皇后
//     if(!row[x] && !col[y] && !dg[x + y] && !udg[x - y + n])
//     {
//         // 如果都没有皇后，则在当前位置放置一个皇后
//         g[x][y] = 'Q';
//         // 标记当前行已经有皇后
//         row[x] = true;
//         // 标记当前列已经有皇后
//         col[y] = true;
//         // 标记当前正对角线已经有皇后
//         dg[x + y] = true;
//         // 标记当前反对角线已经有皇后
//         udg[x - y + n] = true;
//         // 递归调用 dfs 函数，处理下一个格子，同时皇后数量加 1
//         dfs(x, y + 1, s + 1);
//         // 回溯操作，撤销当前位置的皇后放置
//         g[x][y] = '.';
//         // 撤销当前行的标记
//         row[x] = false;
//         // 撤销当前列的标记
//         col[y] = false;
//         // 撤销当前正对角线的标记
//         dg[x + y] = false;
//         // 撤销当前反对角线的标记
//         udg[x - y + n] = false;
//     }
// }

// int main()
// {
//     // 从标准输入读取棋盘的大小 n
//     cin >> n;
//     // 初始化棋盘，将所有位置都置为 '.'，表示空位
//     for(int i = 0; i < n; i++)
//     {
//         for(int j = 0; j < n; j++)
//         {
//             g[i][j] = '.';
//         }
//     }
//     // 从第 0 行第 0 列开始进行深度优先搜索，初始皇后数量为 0
//     dfs(0, 0, 0);
//     // 程序正常结束
//     return 0;
// }