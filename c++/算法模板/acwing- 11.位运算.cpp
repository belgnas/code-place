/*

// 基本思想:

//         我们知道计算机中的数在内存中都是以二进制形式进行存储的 ，而位运算就是直接对整数在内存中的二进制位进行操作，因此其执行效率非常高，在程序中使用位运算进行操作，会大大提高程序的性能。在有些dp等问题中，灵活运用数的二进制来表示某种状态可以大大降低代码的复杂度，位运算很多时候可以将我们的问题简单化，但是并不容易想到，需要一定的积累和对位运算知识的熟练掌握。

//         lowbit原理：根据计算机负数表示的特点，如一个数字原码是011111000，他的负数表示形式是补码，也就是反码+1，反码是10000111，加一则是10001000，二者按位与得到了1000，就是我们想要的lowbit操作，即得到最后一位1

C++ 位运算简介

一、常用位运算

1. 按位与 a & b
法则：两者相同位都为 1，则结果中该位为 1；否则结果中该位为 0
例：
12 & 6 = 4
12：1 1 0 0
6：0 1 1 0
4：0 1 0 0
// 常用于判断某一位是否为1或者清零某些位

2. 按位或 a | b
法则：两者相同位中有一个为 1，则结果中该位为 1；否则结果中该位为 0
例：
12 | 6 = 14
12：1 1 0 0
6：0 1 1 0
14：1 1 1 0
// 常用于设置某一位为1

3. 按位异或 a ^ b
法则：两者相同位的值若不同，则结果中该位为 1；否则结果中该位为 0
例：
12 ^ 6 = 10
12：1 1 0 0
6：0 1 1 0
10：1 0 1 0
// 常用于交换两个变量的值或者求两个数的差异

4. 按位取反 ~a
法则：该数中 0 的位置变为 1；1 的位置变为 0（涉及二进制补码，先不例）
// 常用于获取一个数的补码，注意负数的补码表示

5. 按位左移 a << b
法则：将该数 a 左移 b 位，正数左移后为正数，负数左移后为负数
例：
3 << 2 = 12
3：0 0 1 1
12：1 1 0 0
a << b = a × 2^b
// 常用于快速乘以2的幂次方

6. 按位右移 a >> b
法则：将该数 a 右移 b 位，正数右移后为正数，负数右移后为负数。不论正负，都下取整
例：
13 >> 2 = 3
13：1 1 0 1
3：0 0 1 1
a >> b = a / 2^b
// 常用于快速除以2的幂次方

7. 非 !a
法则：该数是 0 则为 1；否则为 0
例：
!0 = 1
!1 = 0
!2 = 0
// 常用于逻辑判断

二、运算符优先级
-,!,~ >                           // 一元运算符优先级最高
*,/,% >                           // 乘除运算优先级次高
>>,<< >                           // 位移运算优先级
>,<,>=,<= >                       // 关系运算优先级
==,!= >                           // 等值运算优先级
& >                               // 按位与运算优先级
^ >                               // 按位异或运算优先级
| >                               // 按位或运算优先级
&& >                              // 逻辑与运算优先级
|| >                              // 逻辑或运算优先级
问号表达式 >                      // 三元运算符优先级
赋值语句                          // 赋值运算优先级最低

三、负数表示
用补码表示负数：
-1 = 0 - 1
0 = 000...00
1 = 000...01
所以 -1 = 111...11
同理：
-2 = 111...110
-3 = 111...101
-4 = 111...100
...
-x = ~x + 1
// 负数在计算机内部以补码形式存储，补码制能够方便地进行加减运算

四、八进制与十六进制

1. 八进制数
每一位用 0~7 表示，每一位占二进制中的 3 位
在 C++ 中，前面加 0 表示八进制数，例：
printf("%d\n", 015); // 输出：13
// 八进制数方便与二进制数相互转换，适用于某些特定的系统设置或权限管理

2. 十六进制数
每一位用 0~f 表示（a~f 分别表示 10~15），每一位占二进制中的 4 位
在 C++ 中，前面加 0x 表示十六进制数，例：
printf("%d\n", 0xd); // 输出：13
// 十六进制数在编程中广泛用于颜色表示、内存地址等

五、字节
一个字节为 8 位二进制数，也是 2 位十六进制数，即 00000000~11111111
一个 int 占 4 个字节，即 32 位二进制数
一个 long long 占 8 个字节，即 64 位二进制数
memset 按字节赋值，故当 f 为 int 型数组时， memset(f, 0x3f, sizeof f) 是将 f 赋值为 0x3f3f3f3f
// 字节是计算机中数据存储的基本单位，不同的数据类型占用的字节数不同

六、位运算常用技巧（多用于状压 DP）

1. 将 x 第 i 位取反：x ^= 1 << i
// 通过异或操作实现特定位的取反

2. 将 x 第 i 位制成 1：x |= 1 << i
// 通过或操作实现特定位的设置

3. 将 x 第 i 位制成 0：x &= -1 ^ (1 << i) 或 x &= ~(1 << i)
// 通过与操作和取反操作实现特定位的清零

4. 取 x 对 2 取模的结果：x & 1
// 利用二进制最低位判断奇偶性

5. 取 x 的第 i 位是否为 1：x & (1 << i) 或 (x >> i) & 1
// 通过位移和与操作判断特定位的状态

6. 取 x 的最后一位：x & -x
// 利用负数的补码特性获取最低位的1

7. 取 x 的绝对值：(x ^ (x >> 31)) - (x >> 31) （int 型）
// 通过位运算实现绝对值的快速计算

8. 判断 x 是否不为 2 的整次方幂：x & (x - 1)
// 利用二进制中2的幂次方数的特性

9. 判断 a 是否不等于 b：a != b、a - b、a ^ b
// 位运算实现简单的比较操作

10. 判断 x 是否不等于 -1：x != -1、x ^ -1、x + 1、~x
// 多种方式判断数值是否为-1

七、应用

1. 枚举子集
枚举一个集合 S 的子集 O(2^{|S|}) （|S| 表示 S 中的元素个数）
// S 是一个二进制数，表示一个集合，i 枚举 S 的所有子集
for (int i = S; i; i = S & (i - 1))
    // blablabla
// 通过位运算高效地枚举子集，适用于组合问题

枚举集合 0,1,...,n?1 的所有子集的子集 O(3^n)
for (int S = 0; S < (1 << n); S ++ ) // 枚举集合 {0, ..., n - 1} 的所有子集
    for (int i = S; i; i = S & (i - 1)) // 枚举子集 S 的子集
        // blablabla
// 更高层次的子集枚举，适用于复杂的组合问题

2. 交换两个数字
void swap(int &a, int &b)
{
    a ^= b;
    b ^= a;
    a ^= b;
}
// 利用异或操作实现无中间变量的交换

3. 求二进制中数字 1 的个数

（1）暴力 O(logx)
int count(int x)
{
    int res = 0;
    while (x) res += x & 1, x >>= 1;
    return res;
}
// 逐位检查，适用于简单的场景

（2）针对 1 较稀疏的情况的优化 O(logx)
int count(int x)
{
    int res = 0;
    while (x) x &= x - 1, res ++ ;
    return res;
}
// 利用 x & (x-1) 快速消除最低位的1，适用于1较稀疏的情况

（3）位运算 O(1)
int count(int x)
{
    x = (x >> 1 & 0x55555555) + (x & 0x55555555);
    x = (x >> 2 & 0x33333333) + (x & 0x33333333);
    x = (x >> 4 & 0x0f0f0f0f) + (x & 0x0f0f0f0f);
    return x % 255;
}
// 利用位运算并行统计每一位的1，实现常数时间的统计

4. 求 log2x

（1）暴力 O(logx)
int log_2(int x)
{
    int res = 0;
    while (x >> 1) res ++ , x >>= 1;
    return res;
}
// 通过不断右移计算最高位的位置

（2）二分 O(loglogx)
int log_2(int x)
{
    int l = 0, r = 31, mid;
    while (l < r)
    {
        mid = l + r + 1 >> 1;
        if (x >> mid) l = mid;
        else    r = mid - 1;
    }
    return r;
}
// 利用二分法快速定位最高位

（3）位运算（二分展开）O(1)
int log_2(int x)
{
    int res = 0;
    if (x & 0xffff0000) res += 16, x >>= 16;
    if (x & 0xff00) res += 8, x >>= 8;
    if (x & 0xf0) res += 4, x >>= 4;
    if (x & 0xc) res += 2, x >>= 2;
    if (x & 2) res ++ ;
    return res;
}
// 利用位运算分段查找，实现常数时间的计算

（4）预处理 1~n 中所有数 log 后的结果 O(n)
方法一：
int log_2[N];   // 存 log2(i) 的结果
void init(int n)
{
    for (int i = 0; (1 << i) <= n; i ++ )
        log_2[1 << i] = i;
    for (int i = 1; i <= n; i ++ )
        if (!log_2[i])
            log_2[i] = log_2[i - 1];
}
// 预处理log值，适用于需要多次查询的场景

方法二：
int log_2[N];   // 存 log2(i) 的结果
void init(int n)
{
    for (int i = 1; i <= n; i ++ )
        log_2[i] = log_2[i - 1] + ((1 << log_2[i - 1]) == i);
    for (int i = 1; i <= n; i ++ )
        log_2[i] -- ;
}
// 另一种预处理方式，适用于不同的编程习惯

5. 快读优化
inline int read()
{
   int x = 0; bool f = false; char ch = getchar();
   while (ch < 48 || ch > 57) {f |= (ch == 45); ch = getchar();}
   while (ch > 47 && ch < 58) x = (x << 1) + (x << 3) + (ch ^ 48), ch = getchar();
   return f ? ~x + 1 : x;
}
// 利用位运算和异或操作实现快速读取整数，适用于需要高效输入的场景

6. 龟速加
int add(int a, int b)
{
    while (b)
    {
        int x = a ^ b;
        b = (a & b) << 1;
        a = x;
    }
    return a;
}
// 利用位运算模拟加法，适用于理解计算机底层运算原理

*/